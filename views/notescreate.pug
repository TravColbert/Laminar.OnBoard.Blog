
extends index.pug

block content
  section.layout-width-sd.layout-col
    .section-hdg.section-hdg-1 Create Note In: 
    .layout-row.layout-width-1-1
      .item-listing.layout-col.layout-width-1-1.layout-sd-width-5-8.layout-lg-width-5-8.layout-xl-width-4-8
        form(action="/notes/" method="post")
          input(id="user" type="hidden" name="userId" value=user.id)
          input(id="domain" type="hidden" name="domainId" value=user.currentDomain.id)
          .inputnotename.itemdetail.layout-col
            label.label(for="name") Name/Title:
            input#name(type="text" name="name" placeholder="Name or Title" autofocus)
          .inputnotedescription.itemdetail.layout-col
            label.label(for="description") Description:
            input#description(type="text" name="description" placeholder="Description of the note")
          .inputnotepublic.itemdetail.layout-col
            label.label(for="public") Visibility:
            input#public(type="text" name="public" placeholder="Note is public?")
          .inputnotebody.itemdetail.layout-col
            label.label(for="body") Text:
            textarea#body(cols="30" rows="10" name="body")
          .notesubmit.itemdetail.layout-col
            .label Actions:
            .data
              input#submit.btn.normal.buttonconfirm(type="submit" value="Confirm")
              a.btn.cancel.buttoncancel(href="/notes/") Cancel
      .description.layout-col.layout-xs-hidden.layout-sm-hidden.layout-md-hidden.layout-sd-width-3-8.layout-lg-width-3-8.layout-xl-width-4-8
        .infotext.section-hdg.section-hdg-3 Create Note
        .infotext You can add a new note with this view.
        .infotext Enter the details for the part that you would like to add. These details can be edited later.
        .infotext After generating a new part stokr will generate an ID for this part for tracking purposes.
        .infotext.section-hdg.section-hdg-3 Manufacturer
        .infotext Enter the manufacturer's name here. This should be the manufacturer for wich the part should be used.
        .infotext.example.layout-col 
          .infotext.heading Examples:
          .infotext Acer
          .infotext Lenovo
          .infotext HP
        .infotext.important This is a required field.
